import React from 'react';

import { SafeAreaView, StatusBar, Header } from 'components';

export const AccountDocuments = () => (
  <SafeAreaView>
    <StatusBar />
    <Header title="Identity Documents" />
  </SafeAreaView>
);
